@model IEnumerable<StoreFront.DATA.EF.Models.ServerMode>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ServerName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModeName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ServerName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ModeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ServerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ServerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ServerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
